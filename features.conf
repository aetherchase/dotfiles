# Default feature set installed by apply.sh.
# One package name per line; '#' comments and blank lines ignored.
# `core` is always installed regardless of this list.
#
# Per-host override: copy this file to `features.local` (gitignored) and edit.
#   Resolution order: CLI args  >  features.local  >  features.conf
# Drop a feature simply by removing/commenting its line — apply.sh unstows it.

core
swayosd-glass
walker-theme
yazi
waybar
keybind-lookup
